# Heading 1

## Heading 2

### Heading 3

Regular paragraph with **bold**, *italic*, and `inline code` text. Long enough to show line wrapping behaviour at normal readable column width.

---

## After Horizontal Rule

Paragraph immediately after rule — should have correct spacing and left-aligned heading.

- Bullet item one
- Bullet item two with `inline code` that wraps: `VeryLongIdentifier.withSomeLongMethod().chainedCall()`
- Bullet item three

1. Ordered item one
2. Ordered item two

> Blockquote with some text inside.

```swift
func example() -> String {
    return "code block"
}
```
